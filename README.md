# test-time-adaptation

## Installation

1. Install poetry.
    ```bash
    pip install poetry
    ```
2. Install poetry packages.
    ```bash
    poetry install
    ```
3. Run test code.
    ```bash
    python tests/installation_check.py
    ```
